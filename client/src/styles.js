import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    bar: {
      marginBottom: "120px"
    },
      heading: {
        color: 'rgba(0,0,0, 1)',
      },
      image: {
        marginLeft: '15px',
      },
}))